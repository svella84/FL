<div class="row">
  <div class="large-7 columns">
    <h1 class="centra charT">Registrati</h1>
    <img src="/system/image/fl.png">
    <p>Esegui la registrazione con pochi semplici passi per te tanti vantaggi in più.</p>
  </div>

  <div class="large-5 columns">
    <h1 class="centra charT">Sign Up</h1>
    <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>

      <%= f.fields_for(:information, resource.build_information) do |info| %>

        <div><%= info.text_field :name, :placeholder => 'Nome' %></div>

        <div><%= info.text_field :surname, :placeholder => 'Cognome' %></div>

      <% end %>

      <div><%= f.text_field :email, :placeholder => 'E-mail' %></div>

      <div><%= f.password_field :password, :placeholder => 'Password' %></div>

      <div><%= f.password_field :password_confirmation, :placeholder => 'Conferma password' %></div>

      <div><%= f.submit "Registrati", class: "radius button expand" %></div>

      <div class="panel log_reg">
        <%= render "devise/shared/links" %>
      </div>
    <% end %>

  </div>
</div>

