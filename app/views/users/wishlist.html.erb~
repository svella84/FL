<div class="row">
  <div class="large-12 small-12 columns">
<h1>La lista dei tuoi Desideri</h1>

   <%= will_paginate @wishs %>
    <% @wishs.each do |wish| %>

      <table class="large-12 column">
        <tr>
          <td class="large-3 column centra"><%= link_to (image_tag wish.product.image_url.url(:large)), product_path(wish) %></td>
          <td class="large-9 column">
            <h4><%= link_to wish.product.title, product_path(wish) %></h4>
            <h5>Prezzo: <%= number_to_currency(wish.product.price,:unit => "€ ", :separator => ",", :delimiter => ".") %></h5>
            <h5>Disponibilità (kg): <%= number_to_currency(wish.product.qnt, :unit => "", :separator => ",", :delimiter => ".", precision: 3) %></h5>     
          </td>
        </tr>
      </table>    

    <% end %>
    <%= will_paginate @wishs %>

</div>
</div>
