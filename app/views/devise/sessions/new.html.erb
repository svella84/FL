<div class="row">
  <div class="large-7 columns centra">
    <h1 class="centra charT">Login</h1>
    <img src="/system/image/fl.png">
    <p>
      Effettua il Login per poter portare sulla tua tavola i sapori di tutta l'Italia.<br />
      Non sei ancora registrato fallo subito nella sezione SIgn Up.
    </p>
  </div>

  <div class="large-5 columns">
    <h1 class="centra charT">Sign In</h1>
    <%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>

      <div>
        <%= f.email_field :email, :placeholder => 'E-mail' %>
      </div>
      <div>
        <%= f.password_field :password, placeholder: 'Password' %>
      </div>

      <% if devise_mapping.rememberable? -%>
        <div>
          <%= f.check_box :remember_me %> <%= f.label :remember_me, 'Ricordati l\'accesso'%>
        </div>
      <% end -%>

      <div><%= f.submit "Login", class: "radius button expand" %></div>
    <% end %>

    <div class="panel log_reg">
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
